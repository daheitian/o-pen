{
  "name": "pi-mind",
  "version": "1.0.0",
  "description": "Pi-Mind: Card note application with Bun and Pi Agent",
  "scripts": {
    "install:all": "bun install && (cd server && bun install) && (cd client && bun install)",
    "dev:server": "bun --watch server/index.js",
    "dev:client": "cd client && bun run dev",
    "dev": "bun run dev:server & bun run dev:client"
  }
}
